// If set to true, copy the original sheet before deleting rows.
if (condition === true) {
    // Perform the operation to copy the original sheet
    copyOriginalSheet();
    
    // Proceed with deleting rows
    deleteRows();
}
